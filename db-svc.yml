apiVersion: v1
kind: Service
metadata:
  name: mysqldb
  namespace: default
spec:
  # Headless service provides stable DNS for the StatefulSet pod.
  clusterIP: None
  selector:
    app: mysqldb
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
